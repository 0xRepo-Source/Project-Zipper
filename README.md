# Project-Zipper
A lightweight Go CLI that creates zip archives of project folders using only the Go standard library. It generates unique archive names by appending version suffixes when a zip with the base name already exists.
